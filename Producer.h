#ifndef PRODUCER_H
#define PRODUCER_H

#include <sys/types.h>
#include <sys/socket.h>
#include <netinet/in.h>

#include "Sync.h"

class Producer{
public:
  Producer(const char* ip, const char* port);
  Producer
private:
  sockaddr_in 
};

#endif
